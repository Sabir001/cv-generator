import styled from "styled-components";

const GeneratorButton = styled.button`
  padding: 1em;
`;

const Container = styled.div`
  margin-top: 5em;
  text-align: center;
`;

export { GeneratorButton, Container };
